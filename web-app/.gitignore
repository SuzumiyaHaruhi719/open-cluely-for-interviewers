# secrets — never commit
.env
.env.*
!.env.example

# deps / build
node_modules/
dist/
build/
*.log

# NOTE: packages/question-bank/data/ (bank.raw.json, bank.json, vectors.bin) IS
# committed on purpose — it makes the question bank turnkey on clone/deploy
# without needing the DashScope key to re-embed. Regenerate with:
#   npm run scrape && DASHSCOPE_API_KEY=... npm run build-embeddings

# server session file-store (runtime data)
.data/
